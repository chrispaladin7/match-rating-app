<%- include('../partials/header') %>

    <h1>List All Teams</h1>
    <table id="list">
        <thead>
            <tr>
                <th>Logo</th>
                <th>Team Name</th>
                <th>Abbreviation</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <% teams.forEach(function(t){ %>
                <tr>
                    <td><img src="<%= t.logo %>" alt=""></td>
                    <td>
                        <%= t.name %>
                    </td>
                    <td>
                        <%= t.abbreviation %>
                    </td>
                    <td>
                        <a href="teams/<%= t._id %>">click for more</a>
                    </td>
                </tr>
                <% }); %>
        </tbody>
    </table>

    <%- include('../partials/footer') %>